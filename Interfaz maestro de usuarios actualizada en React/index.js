import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
/*const cors = require ('cors'); - pendiente cuando se monte el backend*/
/* app.use(cors()); - pendiente cuando se monte el backend*/
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);


